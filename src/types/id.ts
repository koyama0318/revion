/** Represents the type of an aggregate (e.g., 'order', 'user'). */
export type AggregateType = string

/** Represents the unique identifier of an aggregate instance (typically a UUID). */
export type AggregateId = string
